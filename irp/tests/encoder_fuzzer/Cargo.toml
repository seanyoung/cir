[package]
name = "encoder_fuzzer"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
afl = "*"
irp = { path = "../.." }
irptransmogrifier = { path = "../rust-irptransmogrifier" }
rand = "0.8"

[workspace]
